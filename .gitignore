# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local caches (Macrostrat responses, scratch outputs)
cache/
*.log

# Working notes / throwaway analysis. Not part of the record.
notes/
scratch/

# Interactive folium maps: ~2MB each and fully regenerable via
# scripts/render_map.py. The committed PNG is the one the README shows.
outputs/*.html

# Grid fetch checkpoints (resumption only; the featurized grid is committed)
data/processed/*_raw.csv

# Secrets (none expected, but never risk it)
.env